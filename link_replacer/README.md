# Image Link Replacer

Converts the hashed file name in mark down files to the appropiate name from aa corresponding HTML file

## To use

Place the HTML file in the HTML folder under resources.

Place the markdown file of the same in the md folder under resources.

Run `python main.py` from the main directory.
